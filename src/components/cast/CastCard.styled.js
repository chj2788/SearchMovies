import styled from 'styled-components';
import { SearchCard } from '../Styled';

export const StyledCastCard = styled(SearchCard)`
  .deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`;
